import {Dimensions} from 'react-native';

const {width} = Dimensions.get('window');

const NUMBER = {
  defaultWhatsAppNumber: '6289519705061',
  defaultPaddingHorizontal: 15,
  defaultWidth: width,
  defaultTitleHeaderWidth: width - width * 0.175,
};

export default NUMBER;
